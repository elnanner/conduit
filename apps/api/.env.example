# Copiar este archivo a ".env" en la misma carpeta.
# Estos valores coinciden con las credenciales locales definidas en docker-compose.yml
# (son solo para desarrollo local, no son secretos reales).
DATABASE_URL="postgresql://conduit:conduit@localhost:5432/conduit?schema=public"
